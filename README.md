# Booking_Reservation

Check the next URls:

This view is secured,so its neccesary that you generate a token to see that (you need to generate your token using the path *auth/token/login* using insonia or a web browser)

*api/reservation/booking/table*


*api/menu/*



